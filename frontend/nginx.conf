server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static files; fall back to index.html for React Router routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}
