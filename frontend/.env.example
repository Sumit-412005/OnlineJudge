# Copy this file to ".env" for local development, or set the variable in your
# hosting provider (e.g. Vercel) for production builds.
#
# Base URL of the backend API. Create React App inlines this at BUILD time,
# so you must rebuild after changing it.
REACT_APP_API_URL=http://localhost:4000
